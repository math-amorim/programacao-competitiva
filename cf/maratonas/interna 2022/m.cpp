#include <bits/stdc++.h>
using namespace std; 

int main(){
    int t, l, c; 

    while(t--){
        cin >> l >> c;
        char matriz[l][c], entrada; 
        for(int i = 0; i < l; i++)
            for(int j = 0; j < c; j++){
                cin >> entrada;
                matriz[l][c] = entrada; 
            }

        

    }

    return 0; 
}